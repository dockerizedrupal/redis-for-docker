redis:
  image: simpledrupalcloud/redis:dev
  hostname: redis
  ports:
    - "6379:6379"
  volumes_from:
    - redisdata
redisdata:
  image: simpledrupalcloud/data:dev
  hostname: redisdata
  volumes:
    - ./.dev/redis/data:/redis/data
