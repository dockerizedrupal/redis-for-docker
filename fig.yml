redis:
  image: simpledrupalcloud/redis:latest
  hostname: redis
  ports:
    - "6379:6379"
  volumes_from:
    - redisdata
redisdata:
  image: simpledrupalcloud/data
  hostname: redisdata
  volumes:
    - ./.dev/redis/data:/redis/data
